CREATE TABLE hosts (
  id                TEXT        PRIMARY KEY,
  
  labels            TEXT[]      NOT NULL

/*
  cpu_capacity      INTEGER     NOT NULL,
  memory_capacity   BIGINT      NOT NULL
*/
);

